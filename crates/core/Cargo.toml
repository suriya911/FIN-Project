[package]
name = "tessera-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Deterministic limit order book matching engine. no_std, zero dependencies."

[dependencies]
# Intentionally empty. The engine core depends on nothing.
